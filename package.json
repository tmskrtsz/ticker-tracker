{
  "name": "ticker-tracker-backend",
  "dependencies": {
    "axios": "^0.18.0",
    "body-parser": "^1.18.2",
    "config": "^1.30.0",
    "express": "^4.16.2",
    "mongoose": "^5.0.9",
    "morgan": "^1.9.0",
    "node-binance-api": "^0.4.23",
    "node-telegram-bot-api": "^0.30.0",
    "now-env": "^3.0.4"
  },
  "license": "MIT",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "nodemon index.js",
    "deploy": "now --public && now alias && now remove ticker-tracker-backend --yes --safe"
  },
  "version": "0.0.1"
}
